The first model is the Decision Tree. It divides elements into categories, then makes decisions based on the value of that category. The data will be used to train these value comparisons and categories, which is **fitting** the model. This data is referred to as the **training data**.

---

The decision tree can also "split" into more choices. This allows it to include different factors that contribute to the final result.