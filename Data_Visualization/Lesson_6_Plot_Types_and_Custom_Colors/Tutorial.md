Other than the default style, there are other styles (such as the dark theme) which can be set using ```sns.set_style("dark")```.

The full list of themes are
1. darkgrid
2. whitegrid
3. dark
4. white
5. ticks